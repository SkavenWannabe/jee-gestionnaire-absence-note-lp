CREATE TABLE ETUDIANT (ID NUMBER(10) NOT NULL, MOYENNE NUMBER(12,5), NBABSENCES NUMBER(10), NOM VARCHAR NOT NULL, PRENOM VARCHAR NOT NULL, GROUPE_ID NUMBER(10), PRIMARY KEY (ID))
CREATE TABLE GROUPE (ID NUMBER(10) NOT NULL, NOM VARCHAR NOT NULL UNIQUE, PRIMARY KEY (ID))
ALTER TABLE ETUDIANT ADD CONSTRAINT FK_ETUDIANT_GROUPE_ID FOREIGN KEY (GROUPE_ID) REFERENCES GROUPE (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMBER(19), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
